树
    结点的度
        包含子树的个数
    树的度
        所有结点的度的最大值（不一定是根结点最大）
    树的高度
        最大层次
    孩子结点
    双亲结点（父节点）
    兄弟结点

遍历规则
    根据根结点被访问的顺序分为：
        前序遍历
            根结点->左子树->右子树
        中序遍历
            左子树->根结点->右子树
        后序遍历
            左子树->右子树->根结点
